@model IEnumerable<FurryFeast.Models.StockWarehouseGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>StockWarehouseGroups</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseGroupsCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseGroupsDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WarehouseGroupsCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WarehouseGroupsDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WarehouseGroupsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WarehouseGroupsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WarehouseGroupsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
