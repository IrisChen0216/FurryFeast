@model IEnumerable<FurryFeast.Models.StockSuppliersGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>StockSuppliersGroups</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SuppliersGroupsCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuppliersGroupsDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SuppliersGroupsCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuppliersGroupsDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SuppliersGroupsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SuppliersGroupsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SuppliersGroupsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
